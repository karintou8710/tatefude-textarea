# 切り貼り

`text/plain` だけ。

| 操作 | 動き |
| --- | --- |
| コピー | 選択の文字列。選択が無ければ何もしない |
| 切り取り | 選択を返して消す。`readOnly` なら消さずに返すだけ |
| 貼り付け | 選択を置き換えて入れる |

- `\r\n` と `\r` は `\n` に均す
- `maxLength` を超えるぶんは切る。組み合わさった 1 文字は割らない
